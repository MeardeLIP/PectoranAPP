# 🚀 Быстрое руководство: Как распространить приложение для тестирования

## 📱 Для Android (APK файл)

### Шаг 0: Настройте Android SDK (один раз)

**Перед первой сборкой APK:**

```bash
cd PectoranMobile
node scripts/setup-android-sdk.js
```

Скрипт автоматически найдет и настроит Android SDK.

Если SDK не найден, укажите путь вручную:
```bash
node scripts/setup-android-sdk.js "C:\Users\YourUser\AppData\Local\Android\Sdk"
```

### Шаг 1: Убедитесь, что backend запущен с ngrok

```bash
cd backend
npm run start:ngrok
```

**Важно:** Backend должен быть запущен когда люди используют приложение!

### Шаг 2: Соберите APK файл

**Windows:**
```bash
cd PectoranMobile
npm run build:apk:windows
```

**macOS/Linux:**
```bash
cd PectoranMobile
npm run build:apk
```

**Или вручную:**
```bash
cd PectoranMobile/android
./gradlew assembleRelease
```

APK файл будет в: `PectoranMobile/android/app/build/outputs/apk/release/app-release.apk`

---

## 🍎 Для iOS (IPA файл)

### ⚠️ Важно: Требования для сборки iOS

**Для сборки iOS приложения НЕОБХОДИМО:**
- **Mac с macOS** (Windows не поддерживается для сборки iOS)
- **Xcode** (установлен из App Store)
- **Apple Developer аккаунт** (бесплатный для разработки, платный для распространения)
- **CocoaPods** (устанавливается автоматически)

### Варианты сборки iOS приложения:

#### Вариант 1: Через Xcode (Рекомендуется) ⭐

**Самый простой способ для создания IPA файла:**

1. **Откройте проект в Xcode:**
   ```bash
   cd PectoranMobile/ios
   open PectoranMobile.xcworkspace
   ```
   ⚠️ **Важно:** Открывайте `.xcworkspace`, а не `.xcodeproj`!

2. **Установите зависимости:**
   ```bash
   cd PectoranMobile/ios
   pod install
   ```

3. **В Xcode:**
   - Выберите **Product → Archive**
   - Дождитесь завершения архивации
   - В появившемся окне выберите **Distribute App**
   - Выберите метод распространения:
     - **Ad Hoc** - для установки на конкретные устройства (нужно зарегистрировать UDID)
     - **Enterprise** - для корпоративного распространения (нужен Enterprise аккаунт)
     - **App Store** - для публикации в App Store
     - **Development** - для разработки и тестирования

4. **Экспорт IPA:**
   - Следуйте инструкциям мастера
   - Выберите место для сохранения IPA файла
   - Готово! IPA файл готов к установке

#### Вариант 2: Через скрипт (для симулятора)

**Для тестирования на симуляторе:**

```bash
cd PectoranMobile
npm run build:ios
```

Или для Release сборки:
```bash
npm run build:ios:release
```

**Для установки на реальное устройство:**
```bash
npm run build:ios:device
```

⚠️ **Примечание:** Скрипт создаст архив, но для создания IPA файла лучше использовать Xcode.

#### Вариант 3: Через командную строку (для разработчиков)

**Установка зависимостей:**
```bash
cd PectoranMobile/ios
pod install
```

**Сборка для симулятора:**
```bash
cd PectoranMobile/ios
xcodebuild -workspace PectoranMobile.xcworkspace \
  -scheme PectoranMobile \
  -configuration Release \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  build
```

**Архивация для устройства:**
```bash
cd PectoranMobile/ios
xcodebuild archive \
  -workspace PectoranMobile.xcworkspace \
  -scheme PectoranMobile \
  -configuration Release \
  -archivePath build/PectoranMobile.xcarchive \
  CODE_SIGN_IDENTITY="iPhone Developer" \
  PROVISIONING_PROFILE="your-profile-uuid"
```

### 📋 Пошаговая инструкция для первого раза:

1. **Установите Xcode из App Store**

2. **Откройте терминал и установите CocoaPods:**
   ```bash
   sudo gem install cocoapods
   ```

3. **Установите зависимости проекта:**
   ```bash
   cd PectoranMobile/ios
   pod install
   ```

4. **Откройте проект в Xcode:**
   ```bash
   open PectoranMobile.xcworkspace
   ```

5. **Настройте подпись приложения:**
   - В Xcode выберите проект **PectoranMobile**
   - Перейдите в **Signing & Capabilities**
   - Выберите вашу команду разработчика (Team)
   - Xcode автоматически создаст сертификат и профиль

6. **Подключите iPhone:**
   - Подключите iPhone через USB
   - Доверьтесь компьютеру на iPhone
   - В Xcode выберите ваше устройство в списке устройств

7. **Соберите и установите:**
   - Выберите ваше устройство в Xcode
   - Нажмите **Run** (▶️) или `Cmd + R`
   - Приложение установится на iPhone

8. **Для создания IPA файла:**
   - Выберите **Product → Archive**
   - После архивации: **Distribute App → Ad Hoc**
   - Сохраните IPA файл

### 🚨 Решение проблем:

**Ошибка "No provisioning profile":**
- Зарегистрируйте UDID вашего устройства в Apple Developer
- Выберите правильный Team в Xcode
- Обновите профиль: **Xcode → Preferences → Accounts → Download Manual Profiles**

**Ошибка "Code signing is required":**
- Выберите Team в **Signing & Capabilities**
- Убедитесь, что у вас есть действующий Apple Developer аккаунт

**Ошибка при установке на устройство:**
- На iPhone: **Настройки → Основные → VPN и управление устройством**
- Доверьтесь разработчику приложения

**Не могу собрать на Windows:**
- iOS приложения можно собирать ТОЛЬКО на Mac
- **⚠️ Нет "iOS SDK для Windows"** (в отличие от Android SDK)
- **Решения для Windows:**
  - 🎯 **Codemagic** - самый простой (3 шага, бесплатно!) - [codemagic.io](https://codemagic.io)
  - 🖥️ **Облачный Mac** - аренда Mac в облаке ($20-50/мес) - MacStadium, MacInCloud
  - 👥 **Попросить друга с Mac** - самый быстрый способ
  - 🔧 **GitHub Actions** - автоматическая сборка (бесплатно для публичных репозиториев)
  - 📚 См. все варианты: [`IOS_BUILD_PROGRAMS.md`](./IOS_BUILD_PROGRAMS.md)

**Нужен бесплатный способ тестирования iOS без iPhone?**
- 🆓 **Codemagic** - бесплатный план (500 мин/мес) - собери iOS и отправь заказчику
- 🆓 **GitHub Actions** - бесплатно для публичных репозиториев
- 🆓 **Отправка IPA заказчику** - он установит на свой iPhone (через AltStore/3uTools)
- 📚 Подробности: [`IOS_FREE_TESTING.md`](./IOS_FREE_TESTING.md)

**Есть iPhone и нужно установить приложение?**
- 🎯 **Codemagic + 3uTools** - самый простой способ (бесплатно!)
  1. Собери iOS через Codemagic (10 минут)
  2. Установи на iPhone через 3uTools (2 минуты)
- 📚 Подробная инструкция: [`IOS_BUILD_AND_INSTALL.md`](./IOS_BUILD_AND_INSTALL.md)

### 📦 Альтернативные способы распространения:

1. **TestFlight** (Рекомендуется для тестирования):
   - Загрузите приложение в App Store Connect
   - Добавьте тестировщиков через TestFlight
   - Тестировщики получат ссылку для установки

2. **Ad Hoc Distribution:**
   - Создайте IPA через Xcode
   - Зарегистрируйте UDID устройств тестировщиков
   - Распространите IPA файл (через сайт, email, etc.)
   - Тестировщики устанавливают через iTunes/Finder

3. **Enterprise Distribution:**
   - Требуется Enterprise аккаунт ($299/год)
   - Можно распространять без ограничений
   - Подходит для корпоративного использования

### 💡 Полезные команды:

```bash
# Установка CocoaPods зависимостей
cd PectoranMobile/ios
pod install

# Обновление CocoaPods зависимостей
pod update

# Очистка сборки
cd PectoranMobile/ios
xcodebuild clean -workspace PectoranMobile.xcworkspace -scheme PectoranMobile

# Запуск на симуляторе
cd PectoranMobile
npm run ios

# Запуск на конкретном симуляторе
npm run ios -- --simulator="iPhone 15 Pro"
```

### Шаг 3: Найдите APK файл

После сборки файл будет скопирован в: `PectoranMobile/PectoranAPP-release.apk`

### Шаг 4: Отправьте APK тестировщикам

**Варианты отправки:**
- 📧 Email
- 💬 Мессенджер (Telegram, WhatsApp)
- ☁️ Облачное хранилище (Google Drive, Dropbox)
- 🌐 Файлообменник
- 📱 QR код (создайте ссылку и QR код)

### Шаг 5: Инструкция для тестировщиков

**Отправьте им эту инструкцию:**

1. **Скачайте APK файл** на Android устройство
2. **Разрешите установку:**
   - Откройте Настройки → Безопасность
   - Включите "Установка из неизвестных источников"
   - Или при установке система предложит разрешить
3. **Установите приложение:**
   - Откройте скачанный APK файл
   - Нажмите "Установить"
4. **Запустите приложение:**
   - Откройте PectoranAPP
   - Войдите с вашими учетными данными
5. **Убедитесь, что есть интернет:**
   - Приложение подключается к backend через интернет
   - Не требуется локальная сеть

---

## ⚠️ Важно знать

### Ngrok URL может измениться

Если вы используете **бесплатный план ngrok**, URL меняется при каждом перезапуске:
- При перезапуске backend с ngrok получите новый URL
- Если URL изменился, нужно пересобрать APK с новым URL
- Или используйте платный план ngrok для постоянного URL

### Backend должен быть запущен

- Backend должен работать когда тестировщики используют приложение
- Если backend выключен, приложение не сможет подключиться
- Ngrok туннель должен оставаться активным

### Обновление приложения

Если нужно обновить приложение:
1. Пересоберите APK: `npm run build:apk`
2. Отправьте новый APK тестировщикам
3. Они установят новую версию (старая заменится автоматически)

---

## 🎯 Альтернативные способы

### Способ 1: QR код для скачивания

1. Загрузите APK на Google Drive или Dropbox
2. Создайте публичную ссылку
3. Сгенерируйте QR код с этой ссылкой
4. Отправьте QR код тестировщикам
5. Они сканируют и скачивают

### Способ 2: Веб-страница

Создайте простую HTML страницу со ссылкой на APK и инструкцией по установке.

### Способ 3: Прямая установка через USB

Если тестировщик рядом:
1. Подключите устройство через USB
2. Включите отладку по USB
3. Выполните: `npm run android`

---

## 📝 Полная документация

См. подробное руководство: [`DISTRIBUTION_GUIDE.md`](./DISTRIBUTION_GUIDE.md)

---

## 🆘 Решение проблем

### Проблема: SDK location not found

**Ошибка при сборке:**
```
SDK location not found. Define a valid SDK location...
```

**Решение:**

1. **Настройте Android SDK автоматически:**
   ```bash
   cd PectoranMobile
   node scripts/setup-android-sdk.js
   ```

2. **Или укажите путь вручную:**
   ```bash
   node scripts/setup-android-sdk.js "C:\Users\YourUser\AppData\Local\Android\Sdk"
   ```

3. **Проверьте установку Android Studio:**
   - Убедитесь, что Android Studio установлен
   - Откройте Android Studio → SDK Manager
   - Установите Android SDK, если не установлен

### APK не устанавливается
- Убедитесь, что разрешена установка из неизвестных источников
- Проверьте, что APK не поврежден
- Попробуйте пересобрать APK

### Приложение не подключается
- Убедитесь, что backend запущен: `npm run start:ngrok`
- Проверьте, что ngrok URL правильный в конфигурации
- Проверьте интернет на устройстве

### Ошибка при сборке
1. **Настройте Android SDK:**
   ```bash
   cd PectoranMobile
   node scripts/setup-android-sdk.js
   ```

2. **Очистите кэш:**
   ```bash
   cd android && ./gradlew clean
   # или на Windows:
   cd android && gradlew.bat clean
   ```

3. **Проверьте зависимости:**
   ```bash
   npm install
   ```

4. **Проверьте версию Java:**
   - Требуется Java 17 или выше
   - Проверьте: `java -version`

---

**Готово! Теперь вы можете распространять приложение для тестирования!** 🎉

