# 📦 Руководство по распространению приложения для тестирования

Это руководство поможет вам собрать и распространить мобильное приложение PectoranAPP для тестирования на реальных устройствах.

## 🎯 Варианты распространения

### Android (APK файл) - Рекомендуется ⭐
- ✅ Простой способ
- ✅ Установка через файл
- ✅ Не требует Google Play
- ✅ Подходит для быстрого тестирования

### iOS (IPA файл)
- ⚠️ Требует Apple Developer аккаунт (платный)
- ⚠️ Сложнее в настройке
- ✅ Через TestFlight (рекомендуется)
- ⚠️ Прямая установка возможна только для зарегистрированных устройств

---

## 🤖 Android: Сборка APK файла

### ⚠️ Важно: Настройка Android SDK

**Перед сборкой APK необходимо настроить Android SDK:**

```bash
cd PectoranMobile
node scripts/setup-android-sdk.js
```

Скрипт автоматически:
- Найдет Android SDK на вашем компьютере
- Создаст/обновит файл `android/local.properties`
- Проверит валидность пути

Если SDK не найден автоматически, укажите путь вручную:
```bash
node scripts/setup-android-sdk.js "C:\Users\YourUser\AppData\Local\Android\Sdk"
```

### Вариант 1: Debug APK (для быстрого тестирования)

**Самый простой способ:**

```bash
cd PectoranMobile
npm run android
```

После успешной сборки APK файл будет находиться в:
```
PectoranMobile/android/app/build/outputs/apk/debug/app-debug.apk
```

### Вариант 2: Release APK (рекомендуется для тестирования)

**Для распространения лучше использовать release версию:**

1. **Настройте подпись (один раз):**

Создайте файл `PectoranMobile/android/keystore.properties`:

```properties
MYAPP_RELEASE_STORE_FILE=my-release-key.keystore
MYAPP_RELEASE_KEY_ALIAS=my-key-alias
MYAPP_RELEASE_STORE_PASSWORD=ваш_пароль_хранилища
MYAPP_RELEASE_KEY_PASSWORD=ваш_пароль_ключа
```

2. **Создайте keystore (один раз):**

```bash
cd PectoranMobile/android/app
keytool -genkeypair -v -storetype PKCS12 -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
```

3. **Соберите Release APK:**

```bash
cd PectoranMobile/android
./gradlew assembleRelease
```

APK файл будет находиться в:
```
PectoranMobile/android/app/build/outputs/apk/release/app-release.apk
```

### Вариант 3: Упрощенная сборка (без подписи)

Если вы хотите быстро собрать APK без настройки подписи:

1. Отредактируйте `PectoranMobile/android/app/build.gradle`:

Найдите секцию `android` и добавьте:

```gradle
android {
    ...
    buildTypes {
        release {
            signingConfig signingConfigs.debug
            ...
        }
    }
}
```

2. Соберите APK:

```bash
cd PectoranMobile/android
./gradlew assembleRelease
```

**Внимание:** Такой APK будет работать, но не рекомендуется для production.

---

## 📱 Распространение APK файла

### Способ 1: Прямая отправка файла

1. **Найдите APK файл:**
   - Debug: `PectoranMobile/android/app/build/outputs/apk/debug/app-debug.apk`
   - Release: `PectoranMobile/android/app/build/outputs/apk/release/app-release.apk`

2. **Отправьте файл тестировщикам:**
   - Через email
   - Через мессенджер (Telegram, WhatsApp)
   - Через облачное хранилище (Google Drive, Dropbox)
   - Через файлообменник

3. **Инструкция для установки:**
   - Скачайте APK файл на Android устройство
   - Откройте файл (может потребоваться разрешение на установку из неизвестных источников)
   - Разрешите установку
   - Установите приложение

### Способ 2: QR код для скачивания

1. **Загрузите APK на файлообменник** (например, Google Drive)

2. **Создайте публичную ссылку**

3. **Сгенерируйте QR код** с этой ссылкой:
   - Используйте онлайн сервис: https://www.qr-code-generator.com/
   - Или через npm: `npx qrcode "ваша_ссылка"`

4. **Отправьте QR код тестировщикам:**
   - Они сканируют QR код
   - Скачивают APK
   - Устанавливают приложение

### Способ 3: Веб-страница для скачивания

Создайте простую HTML страницу:

```html
<!DOCTYPE html>
<html>
<head>
    <title>PectoranAPP - Скачать</title>
</head>
<body>
    <h1>PectoranAPP</h1>
    <p>Скачайте приложение для тестирования:</p>
    <a href="app-release.apk" download>Скачать APK</a>
    <p>Инструкция по установке:</p>
    <ol>
        <li>Скачайте APK файл</li>
        <li>Разрешите установку из неизвестных источников</li>
        <li>Установите приложение</li>
    </ol>
</body>
</html>
```

Загрузите на любой хостинг или используйте GitHub Pages.

---

## 🔧 Настройка для тестирования

### Важно: Настройка backend URL

Перед распространением APK убедитесь, что:

1. **Backend запущен с ngrok:**
   ```bash
   cd backend
   npm run start:ngrok
   ```

2. **Конфигурация обновлена:**
   - Файл `PectoranMobile/src/config/network.js` должен содержать ngrok URL
   - Или используйте переменную окружения

3. **Для постоянного URL (рекомендуется для тестирования):**
   - Используйте платный план ngrok для постоянного домена
   - Или обновляйте APK при изменении ngrok URL

### Альтернатива: Использование переменной окружения

Вы можете создать версию приложения, которая позволяет вводить backend URL в настройках:

1. Добавьте экран настроек в приложение
2. Сохраняйте backend URL в AsyncStorage
3. Используйте сохраненный URL вместо константы

---

## 📲 Установка на устройства

### Android

1. **Включите установку из неизвестных источников:**
   - Настройки → Безопасность → Неизвестные источники (включить)
   - Или при установке система предложит разрешить

2. **Скачайте APK файл**

3. **Откройте файл** и установите

4. **Запустите приложение**

### iOS

Для iOS требуется:

1. **Apple Developer аккаунт** ($99/год)

2. **Сборка через Xcode:**
   ```bash
   cd PectoranMobile/ios
   pod install
   cd ..
   npm run ios
   ```

3. **Распространение через TestFlight:**
   - Загрузите в App Store Connect
   - Пригласите тестировщиков через TestFlight
   - Они получат уведомление и смогут установить

---

## 🚀 Быстрая инструкция для тестировщиков

### Для Android:

1. **Скачайте APK файл** (получите ссылку от разработчика)

2. **Разрешите установку:**
   - Откройте Настройки → Безопасность
   - Включите "Неизвестные источники"

3. **Установите приложение:**
   - Откройте скачанный APK файл
   - Нажмите "Установить"

4. **Запустите приложение:**
   - Откройте PectoranAPP
   - Войдите с вашими учетными данными

5. **Убедитесь, что есть интернет:**
   - Приложение подключается к backend через интернет
   - Не требуется локальная сеть

---

## 🔄 Обновление приложения

### Когда нужно обновить APK:

- При изменении ngrok URL (если используется бесплатный план)
- При добавлении новых функций
- При исправлении ошибок

### Процесс обновления:

1. **Соберите новый APK:**
   ```bash
   cd PectoranMobile/android
   ./gradlew assembleRelease
   ```

2. **Отправьте новый APK тестировщикам**

3. **Тестировщики устанавливают новую версию:**
   - Старая версия автоматически заменится
   - Или удалите старую версию и установите новую

---

## 📝 Скрипты для автоматизации

### Создайте скрипт для быстрой сборки:

`PectoranMobile/scripts/build-apk.sh`:

```bash
#!/bin/bash
echo "🔨 Сборка APK файла..."

cd android
./gradlew assembleRelease

if [ $? -eq 0 ]; then
    echo "✅ APK собран успешно!"
    echo "📁 Файл: android/app/build/outputs/apk/release/app-release.apk"
    
    # Копируем в удобное место
    cp app/build/outputs/apk/release/app-release.apk ../PectoranAPP-release.apk
    echo "📦 Скопировано в: PectoranAPP-release.apk"
else
    echo "❌ Ошибка сборки APK"
    exit 1
fi
```

Для Windows (`PectoranMobile/scripts/build-apk.bat`):

```bat
@echo off
echo 🔨 Сборка APK файла...

cd android
call gradlew.bat assembleRelease

if %ERRORLEVEL% EQU 0 (
    echo ✅ APK собран успешно!
    echo 📁 Файл: android\app\build\outputs\apk\release\app-release.apk
    
    copy app\build\outputs\apk\release\app-release.apk ..\PectoranAPP-release.apk
    echo 📦 Скопировано в: PectoranAPP-release.apk
) else (
    echo ❌ Ошибка сборки APK
    exit /b 1
)
```

Добавьте в `package.json`:

```json
{
  "scripts": {
    "build:apk": "cd android && ./gradlew assembleRelease",
    "build:apk:windows": "cd android && gradlew.bat assembleRelease"
  }
}
```

---

## 🎯 Рекомендации

### Для быстрого тестирования:
- ✅ Используйте Debug APK
- ✅ Распространяйте через файлообменник
- ✅ Используйте ngrok для backend

### Для серьезного тестирования:
- ✅ Используйте Release APK
- ✅ Настройте подпись приложения
- ✅ Используйте постоянный ngrok домен (платный план)
- ✅ Создайте веб-страницу для скачивания

### Для production:
- ✅ Используйте Google Play (Android)
- ✅ Используйте App Store (iOS)
- ✅ Настройте CI/CD для автоматической сборки
- ✅ Используйте собственный backend сервер

---

## 🆘 Решение проблем

### Проблема: SDK location not found

**Ошибка:**
```
SDK location not found. Define a valid SDK location with an ANDROID_HOME environment variable or by setting the sdk.dir path in your project's local properties file.
```

**Решение:**

1. **Автоматическая настройка (рекомендуется):**
   ```bash
   cd PectoranMobile
   node scripts/setup-android-sdk.js
   ```

2. **Ручная настройка:**
   ```bash
   cd PectoranMobile
   node scripts/setup-android-sdk.js "C:\Users\YourUser\AppData\Local\Android\Sdk"
   ```
   Замените путь на ваш путь к Android SDK.

3. **Через переменные окружения:**
   - Windows: `setx ANDROID_HOME "C:\Users\YourUser\AppData\Local\Android\Sdk"`
   - macOS/Linux: `export ANDROID_HOME=$HOME/Library/Android/sdk`

4. **Проверка установки Android SDK:**
   - Убедитесь, что Android Studio установлен
   - Откройте Android Studio → SDK Manager
   - Установите Android SDK, если не установлен
   - Скопируйте путь к SDK (обычно показан в SDK Manager)

### Проблема: APK не устанавливается

**Решение:**
- Убедитесь, что разрешена установка из неизвестных источников
- Проверьте, что APK не поврежден
- Попробуйте пересобрать APK

### Проблема: Приложение не подключается к backend

**Решение:**
- Убедитесь, что backend запущен с ngrok
- Проверьте ngrok URL в конфигурации приложения
- Проверьте интернет соединение на устройстве

### Проблема: Ошибка при сборке APK

**Решение:**
1. **Проверьте настройку Android SDK:**
   ```bash
   cd PectoranMobile
   node scripts/setup-android-sdk.js
   ```

2. **Очистите кэш:**
   ```bash
   cd PectoranMobile/android
   ./gradlew clean
   # или на Windows:
   gradlew.bat clean
   ```

3. **Проверьте зависимости:**
   ```bash
   cd PectoranMobile
   npm install
   ```

4. **Проверьте версию Java:**
   - Требуется Java 17 или выше
   - Проверьте: `java -version`

5. **Проверьте Android SDK:**
   - Убедитесь, что Android Studio установлен
   - Установите Android SDK через SDK Manager
   - Установите Android SDK Build-Tools

### Проблема: Ошибка выполнения gradlew.bat

**Решение:**
- Убедитесь, что файл `android/gradlew.bat` существует
- Проверьте права доступа к файлу
- Попробуйте запустить напрямую: `cd android && gradlew.bat assembleRelease`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Проверьте документацию React Native
4. Обратитесь к разработчику

---

**Готово! Теперь вы можете распространять приложение для тестирования!** 🎉

