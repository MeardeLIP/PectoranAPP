# 🚀 Руководство по запуску PectoranAPP

## ✅ Что уже готово

- ✅ Backend сервер (Node.js + Express + PostgreSQL)
- ✅ Мобильное приложение (React Native)
- ✅ TV Display (React веб-приложение)
- ✅ База данных PostgreSQL настроена
- ✅ Пользователь `pectoranuser` создан
- ✅ Файл `.env` готов к созданию

## 🔧 Шаг 1: Создание файла .env

Создайте файл `backend/.env` со следующим содержимым:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pectoran_restaurant
DB_USER=pectoranuser
DB_PASSWORD=Adnjkpt12354v

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=pectoran_super_secret_key_2024_restaurant_management_system_secure
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3000
NODE_ENV=development

# Initial Data
SEED_DATABASE=true
```

## 🧪 Шаг 2: Тестирование подключения

```bash
cd backend
npm run test-connection
```

Если все настроено правильно, вы увидите:
- ✅ Подключение к PostgreSQL успешно
- ✅ Таблицы синхронизированы
- 📊 Статистика базы данных
- 👥 Пользователи по ролям

## 🚀 Шаг 3: Запуск Backend

### Для локальной разработки (эмулятор)

```bash
cd backend
npm run dev
```

### Для тестирования на реальных устройствах

#### Вариант 1: Через Ngrok туннель (рекомендуется) ⭐

Тестирование из любого места без настройки сети:

```bash
cd backend
npm run start:ngrok
```

Скрипт автоматически:
- Запустит backend сервер
- Создаст ngrok туннель
- Получит публичный URL
- Обновит конфигурацию мобильного приложения

**Требования:**
1. Установите ngrok: `npm install` (пакет установится автоматически)
2. Получите токен на https://ngrok.com/ (бесплатно)
3. Добавьте токен в `backend/.env`: `NGROK_AUTHTOKEN=your_token_here`

#### Вариант 2: Через локальную сеть Wi-Fi

```bash
cd backend
npm run start:network
```

Скрипт автоматически выведет локальный IP адрес для настройки мобильного приложения.

Ожидаемый результат:
```
✅ Подключение к PostgreSQL успешно
✅ Тестовые данные инициализированы
⚠️ Redis недоступен, работаем без кэширования (если Redis не установлен)
🚀 Сервер запущен на порту 3000
📱 API доступно по адресу:
   - Локально: http://localhost:3000/api
   - В сети: http://192.168.1.100:3000/api
🔌 WebSocket доступен на:
   - Локально: ws://localhost:3000
   - В сети: ws://192.168.1.100:3000
📡 Для тестирования на реальных устройствах используйте IP: 192.168.1.100
```

## 📺 Шаг 4: Запуск TV Display

```bash
cd tv-display
npm start
```

Откройте браузер: http://localhost:3001

## 📱 Шаг 5: Запуск Mobile App

### Для Android эмулятора (по умолчанию)

```bash
cd PectoranMobile
npm run setup:network:emulator
npm run android
```

### Для реальных устройств (тестирование на телефонах)

См. подробные инструкции в [`TESTING_ON_REAL_DEVICES.md`](./TESTING_ON_REAL_DEVICES.md)

#### Вариант 1: Через Ngrok (рекомендуется) ⭐

**Быстрый старт:**
1. Установите ngrok и настройте токен (см. выше)
2. Запустите backend с ngrok:
   ```bash
   cd backend
   npm run start:ngrok
   ```
   Конфигурация обновится автоматически!

3. Запустите приложение:
   ```bash
   cd PectoranMobile
   npm run android
   # или
   npm run ios
   ```

4. Тестируйте с любого устройства из любой сети! 🎉

#### Вариант 2: Через локальную сеть Wi-Fi

**Быстрый старт:**
1. Запустите backend с сетевой информацией:
   ```bash
   cd backend
   npm run start:network
   ```
   Запишите локальный IP адрес из вывода.

2. Настройте мобильное приложение:
   ```bash
   cd PectoranMobile
   npm run setup:network:auto
   ```

3. Запустите приложение:
   ```bash
   npm run android
   # или
   npm run ios
   ```

4. Убедитесь, что телефон и компьютер в одной Wi-Fi сети!

## 🔑 Тестовые аккаунты

### Официанты (вход по логину, без пароля)
- **Логин**: `waiter1` - Анна Смирнова
- **Логин**: `waiter2` - Дмитрий Петров  
- **Логин**: `waiter3` - Елена Козлова
- **Логин**: `waiter4` - Михаил Волков

### Повара (вход по логину, без пароля)
- **Логин**: `cook1` - Игорь Шеф-повар
- **Логин**: `cook2` - Ольга Су-шеф

### Администратор (вход по логину, без пароля)
- **Логин**: `admin`

### Директор (вход по логину и паролю)
- **Логин**: `director`
- **Пароль**: `admin123`

## 🍽️ Тестовое меню

Система автоматически создаст:
- **5 категорий**: Горячие блюда, Салаты, Напитки, Десерты, Алкоголь
- **12 позиций**: Стейк Рибай, Паста Карбонара, Цезарь, Кофе, Тирамису и др.

## 🔴 Redis (опционально)

Если хотите использовать кэширование:
1. Установите Redis (см. `REDIS_SETUP.md`)
2. Запустите Redis: `redis-server`
3. Перезапустите backend

## 🛠️ Устранение проблем

### Ошибка подключения к БД
- Проверьте, что PostgreSQL запущен
- Убедитесь, что база `pectoran_restaurant` создана
- Проверьте права пользователя `pectoranuser`

### Ошибка запуска Mobile App
- Убедитесь, что Android Studio установлен
- Проверьте, что эмулятор/устройство подключено
- Запустите: `npx react-native doctor`

### Ошибка запуска TV Display
- Проверьте, что порт 3001 свободен
- Убедитесь, что зависимости установлены: `npm install`

### Проблемы с подключением на реальных устройствах

#### Для ngrok:
- Проверьте установку ngrok: `cd backend && npm run check:ngrok`
- Убедитесь, что токен настроен в `.env` файле
- Убедитесь, что backend запущен: `npm run start:ngrok`
- Проверьте ngrok статус на http://localhost:4040

#### Для локальной сети:
- Убедитесь, что backend запущен: `npm run start:network`
- Проверьте, что устройства в одной Wi-Fi сети
- Проверьте firewall - порт 3000 должен быть доступен
- Получите IP адрес: `cd backend && npm run get-ip`
- Настройте приложение: `cd PectoranMobile && npm run setup:network:auto`

**Рекомендация:** Используйте ngrok для избежания проблем с сетью и firewall!

См. подробные инструкции в [`TESTING_ON_REAL_DEVICES.md`](./TESTING_ON_REAL_DEVICES.md)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `.env`
4. Обратитесь к документации в `README.md`

---

**🎉 Готово! Система PectoranAPP запущена и готова к использованию!**
