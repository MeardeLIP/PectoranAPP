# 📱 Тестирование на реальных устройствах

Это руководство поможет вам настроить и протестировать мобильное приложение PectoranAPP на реальных телефонах без установки на сервер.

## 🎯 Цель

Позволить разработчикам и тестировщикам тестировать приложение на реальных устройствах из любого места, используя backend, запущенный на вашем компьютере.

## 🌐 Варианты подключения

### Вариант 1: Ngrok туннель (рекомендуется) ⭐
- ✅ Тестирование из любого места (не требуется одна сеть)
- ✅ Решает проблемы с firewall
- ✅ Работает через интернет
- ✅ Автоматическая настройка

### Вариант 2: Локальная сеть Wi-Fi
- ✅ Быстрее (локальное подключение)
- ❌ Требует одну Wi-Fi сеть
- ❌ Проблемы с firewall
- ❌ Не работает удаленно

## 📋 Требования

- Компьютер с запущенным backend сервером
- Node.js установлен на компьютере
- Мобильные устройства (Android/iOS)
- Для ngrok: аккаунт на ngrok.com (бесплатный)

---

## 🌐 Вариант 1: Тестирование через Ngrok (рекомендуется)

### Преимущества
- Тестирование из любого места мира
- Нет проблем с firewall
- Не требуется одна Wi-Fi сеть
- Автоматическая настройка

### Шаг 1: Установка и настройка ngrok

#### Вариант A: Установка npm пакета (рекомендуется)

```bash
cd backend
npm install
```

Пакет `@ngrok/ngrok` будет установлен автоматически.

#### Вариант B: Установка ngrok CLI

1. Скачайте ngrok с https://ngrok.com/download
2. Распакуйте и добавьте в PATH
3. Или установите через пакетный менеджер вашей ОС

### Шаг 2: Получение ngrok токена

1. Зарегистрируйтесь на https://ngrok.com/ (бесплатно)
2. Перейдите в Dashboard → Your Authtoken
3. Скопируйте токен

### Шаг 3: Настройка токена

Добавьте токен в файл `backend/.env`:

```env
NGROK_AUTHTOKEN=your_token_here
```

Или установите через CLI:

```bash
ngrok authtoken your_token_here
```

### Шаг 4: Запуск backend с ngrok

```bash
cd backend
npm run start:ngrok
```

Скрипт автоматически:
- Запустит backend сервер
- Создаст ngrok туннель
- Получит публичный URL
- Обновит конфигурацию мобильного приложения

**Пример вывода:**
```
🌐 Запуск backend с ngrok туннелем
════════════════════════════════════════════════════════════
📍 Порт: 3000
🔑 Token: ✅ Настроен
════════════════════════════════════════════════════════════

🚀 Запуск backend сервера...
✅ Ngrok туннель создан: https://abc123.ngrok.io

📱 Информация для мобильного приложения:
════════════════════════════════════════════════════════════
🌐 Ngrok URL: https://abc123.ngrok.io
📡 API URL: https://abc123.ngrok.io/api
🔌 WebSocket: wss://abc123.ngrok.io
════════════════════════════════════════════════════════════

✅ Конфигурация мобильного приложения обновлена
```

### Шаг 5: Запустить мобильное приложение

Конфигурация уже обновлена автоматически! Просто запустите:

```bash
cd PectoranMobile
npm run android
# или
npm run ios
```

### Шаг 6: Тестировать с любого устройства

Теперь вы можете тестировать приложение с любого устройства из любой сети!
- Отправьте ngrok URL тестировщикам
- Они могут тестировать из любого места
- Не требуется настройка firewall
- Не требуется одна Wi-Fi сеть

### Проверка ngrok

Проверить статус ngrok:

```bash
cd backend
npm run check:ngrok
```

---

## 📡 Вариант 2: Тестирование через локальную сеть Wi-Fi

## 🚀 Быстрый старт

### Шаг 1: Запустить backend сервер

```bash
cd backend
npm run start:network
```

Скрипт автоматически определит ваш локальный IP адрес и выведет его в консоль. Запишите этот IP адрес.

**Пример вывода:**
```
🚀 Запуск backend сервера для тестирования на реальных устройствах
════════════════════════════════════════════════════════════
📍 Локальный IP: 192.168.1.100
🔌 Порт: 3000
📱 API URL: http://192.168.1.100:3000/api
🔗 WebSocket URL: ws://192.168.1.100:3000
════════════════════════════════════════════════════════════
```

### Шаг 2: Настроить мобильное приложение

#### Вариант A: Автоматическая настройка (рекомендуется)

```bash
cd PectoranMobile
npm run setup:network:auto
```

Скрипт автоматически определит IP адрес и обновит конфигурацию.

#### Вариант B: Ручная настройка

```bash
cd PectoranMobile
npm run setup:network 192.168.1.100
```

Замените `192.168.1.100` на ваш локальный IP адрес из шага 1.

#### Вариант C: Для Android эмулятора

```bash
cd PectoranMobile
npm run setup:network:emulator
```

### Шаг 3: Убедиться, что устройства в одной сети

- Компьютер и телефоны должны быть подключены к одной Wi-Fi сети
- Проверьте настройки firewall - порт 3000 должен быть доступен
- На Windows: разрешите входящие подключения для Node.js в брандмауэре

### Шаг 4: Запустить мобильное приложение

```bash
cd PectoranMobile
npm run android
# или
npm run ios
```

## 🔧 Детальная настройка

### Получение локального IP адреса вручную

#### Windows:
```bash
ipconfig
```
Ищите "IPv4 Address" в разделе "Wi-Fi" или "Ethernet adapter"

#### macOS/Linux:
```bash
ifconfig
# или
ip addr show
```
Ищите адрес в разделе "inet" (не 127.0.0.1)

### Настройка конфигурации вручную

Если скрипты не работают, вы можете вручную отредактировать файл:

`PectoranMobile/src/config/network.js`

Измените строку:
```javascript
export const LOCAL_IP_ADDRESS = '10.0.2.2'; // Для эмулятора
```

На:
```javascript
export const LOCAL_IP_ADDRESS = '192.168.1.100'; // Ваш локальный IP
```

### Проверка подключения

1. Убедитесь, что backend запущен и доступен:
   ```bash
   curl http://192.168.1.100:3000/api/health
   ```
   Замените IP на ваш.

2. Проверьте endpoint информации о сети:
   ```bash
   curl http://192.168.1.100:3000/api/network-info
   ```

3. На телефоне откройте браузер и перейдите на:
   ```
   http://192.168.1.100:3000/api/health
   ```
   Должен вернуться JSON ответ со статусом "OK".

## 🛠️ Устранение проблем

### Проблема: Приложение не может подключиться к серверу

**Решение:**
1. Проверьте, что backend запущен: `npm run start:network`
2. Убедитесь, что IP адрес правильный в `PectoranMobile/src/config/network.js`
3. Проверьте, что устройства в одной Wi-Fi сети
4. Проверьте firewall на компьютере - разрешите порт 3000
5. Попробуйте пинг с телефона на IP компьютера (используйте приложение для пинга)

### Проблема: Firewall блокирует подключения

**Windows:**
1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Разрешить взаимодействие с приложением"
3. Найдите Node.js и разрешите для частных сетей

**macOS:**
1. Системные настройки → Безопасность → Брандмауэр
2. Разрешите входящие подключения для Node.js

**Linux:**
```bash
sudo ufw allow 3000/tcp
```

### Проблема: IP адрес изменился

Если ваш IP адрес изменился (например, после переподключения к Wi-Fi):
1. Запустите `npm run get-ip` в папке backend для получения нового IP
2. Обновите конфигурацию: `npm run setup:network:auto` в папке PectoranMobile
3. Перезапустите приложение

### Проблема: CORS ошибки

Backend настроен для разрешения всех источников в development режиме. Если видите CORS ошибки:
1. Убедитесь, что `NODE_ENV` не установлен в `production`
2. Проверьте, что backend запущен с помощью `npm run start:network` или `npm run dev`

### Проблема: WebSocket не подключается

1. Проверьте, что WebSocket URL правильный в конфигурации
2. Убедитесь, что порт 3000 не заблокирован firewall
3. Проверьте логи backend на наличие ошибок WebSocket

## 📱 Тестирование разных ролей

После успешного подключения вы можете тестировать все роли на разных устройствах:

### Официант (Waiter)
- Вход по логину (без пароля)
- Создание заказов
- Просмотр своих заказов
- Изменение статуса заказов

### Повар (Cook)
- Вход по логину (без пароля)
- Просмотр активных заказов
- Изменение статуса заказов
- Отметка блюд как готовых

### Администратор (Admin)
- Вход по логину (без пароля)
- Просмотр неоплаченных заказов
- Отметка заказов как оплаченных

### Директор (Director)
- Вход по логину и паролю
- Полный доступ ко всем функциям
- Просмотр статистики
- Управление пользователями

## 🔒 Безопасность

⚠️ **Важно:** Эти настройки предназначены только для development и тестирования!

- Не используйте эти настройки в production
- В production используйте HTTPS и строгие CORS правила
- Не используйте открытый доступ к backend в публичных сетях

## 📝 Дополнительные команды

### Ngrok команды

#### Проверить установку ngrok
```bash
cd backend
npm run check:ngrok
```

#### Запустить backend с ngrok
```bash
cd backend
npm run start:ngrok
```

#### Запустить backend с ngrok в dev режиме
```bash
cd backend
npm run dev:ngrok
```

#### Настроить приложение с ngrok URL
```bash
cd PectoranMobile
npm run setup:network https://abc123.ngrok.io
# или
npm run setup:network abc123.ngrok.io
```

### Локальная сеть команды

#### Получить локальный IP адрес
```bash
cd backend
npm run get-ip
```

#### Запустить backend с выводом сетевой информации
```bash
cd backend
npm run start:network
```

#### Запустить backend в dev режиме с сетевой информацией
```bash
cd backend
npm run dev:network
```

#### Настроить сеть для эмулятора
```bash
cd PectoranMobile
npm run setup:network:emulator
```

#### Настроить сеть автоматически
```bash
cd PectoranMobile
npm run setup:network:auto
```

## 🎉 Готово!

Теперь вы можете тестировать приложение на реальных устройствах!

### С ngrok:
- ✅ Тестирование из любого места
- ✅ Нет проблем с firewall
- ✅ Не требуется одна сеть
- ✅ Автоматическая настройка

### С локальной сетью:
- ✅ Быстрое подключение
- ✅ Работает в локальной сети
- ❌ Требует одну Wi-Fi сеть
- ❌ Могут быть проблемы с firewall

## 🆘 Решение проблем

Если у вас возникли проблемы, проверьте:

### Для ngrok:
1. Проверьте, что ngrok установлен: `npm run check:ngrok`
2. Проверьте, что токен настроен в `.env` файле
3. Проверьте логи backend сервера
4. Убедитесь, что backend запущен: `npm run start:ngrok`
5. Проверьте ngrok статус на http://localhost:4040

### Для локальной сети:
1. Логи backend сервера
2. Логи мобильного приложения (React Native Debugger)
3. Настройки firewall
4. Подключение к Wi-Fi сети
5. Убедитесь, что устройства в одной сети

---

## 💡 Рекомендации

- **Для тестирования из разных мест**: используйте ngrok
- **Для быстрого тестирования в офисе**: используйте локальную сеть
- **Для демонстрации заказчикам**: используйте ngrok (не требуется настройка сети)
- **Для разработки**: используйте локальную сеть (быстрее)

---

**Примечание:** 
- Ngrok бесплатный план предоставляет временные URL, которые меняются при каждом запуске
- Для постоянного URL нужна платная подписка ngrok
- В production всегда используйте HTTPS и строгие CORS правила

