# 🔴 Настройка Redis (опционально)

Redis используется для кэширования данных и повышения производительности системы. Система может работать без Redis, но с предупреждениями в логах.

## 🚀 Быстрая установка Redis для Windows

### Способ 1: Через Chocolatey (рекомендуется)
```bash
# Установите Chocolatey, если не установлен
# Затем установите Redis
choco install redis-64

# Запустите Redis
redis-server
```

### Способ 2: Скачать с GitHub
1. Перейдите на [GitHub Releases](https://github.com/microsoftarchive/redis/releases)
2. Скачайте `Redis-x64-3.0.504.msi`
3. Установите с настройками по умолчанию
4. Запустите Redis через меню Пуск

### Способ 3: Docker (если установлен Docker)
```bash
# Запустите Redis в контейнере
docker run -d --name redis -p 6379:6379 redis:alpine

# Проверьте, что Redis работает
docker ps
```

## 🔧 Настройка

После установки Redis:

1. **Проверьте, что Redis запущен:**
   ```bash
   redis-cli ping
   # Должно вернуть: PONG
   ```

2. **Redis будет автоматически подключен** при запуске backend сервера

3. **Если Redis не установлен** - система будет работать без кэширования (с предупреждениями в логах)

## 🧪 Тестирование

После установки Redis запустите backend:

```bash
cd backend
npm run dev
```

В логах должно появиться:
```
✅ Подключение к Redis успешно
```

## 🛠️ Устранение проблем

### Redis не запускается
- Проверьте, что порт 6379 свободен
- Убедитесь, что Redis установлен корректно
- Попробуйте запустить от имени администратора

### Ошибка подключения
- Проверьте, что Redis запущен: `redis-cli ping`
- Убедитесь, что в `.env` указан правильный URL: `REDIS_URL=redis://localhost:6379`

### Альтернатива
Если Redis не удается установить, система будет работать без кэширования. Это не критично для разработки и тестирования.
