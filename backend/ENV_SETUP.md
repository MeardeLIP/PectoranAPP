# 🔧 Настройка переменных окружения

## Создание файла .env

Создайте файл `backend/.env` со следующим содержимым:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pectoran_restaurant
DB_USER=pectoranuser
DB_PASSWORD=Adnjkpt12354v

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=pectoran_super_secret_key_2024_restaurant_management_system_secure
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3000
NODE_ENV=development

# Initial Data
SEED_DATABASE=true
```

## Инструкция по созданию:

1. Откройте папку `backend/`
2. Создайте новый файл с именем `.env` (с точкой в начале)
3. Скопируйте содержимое выше в этот файл
4. Сохраните файл

## Проверка настроек:

После создания файла `.env`:

1. Убедитесь, что PostgreSQL запущен
2. Проверьте, что база данных `pectoran_restaurant` создана
3. Убедитесь, что пользователь `pectoranuser` имеет права доступа
4. Redis можно установить позже или временно отключить

## Запуск:

```bash
cd backend
npm run dev
```

Если все настроено правильно, вы увидите сообщение о успешном подключении к базе данных!
