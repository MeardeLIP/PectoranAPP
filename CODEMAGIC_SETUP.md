# 🚀 Настройка Codemagic для сборки iOS

## Быстрая инструкция

### 1. Создай GitHub репозиторий

**Если репозитория еще нет:**

В PowerShell в корне проекта `K:\PectoranAPP`:

```powershell
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Создай репозиторий на GitHub (через браузер):
# 1. Зайди на https://github.com
# 2. Нажми "New repository"
# 3. Назови репозиторий (например: PectoranAPP)
# 4. НЕ добавляй README, .gitignore (они уже есть)
# 5. Скопируй URL репозитория (например: https://github.com/твой-username/PectoranAPP.git)

# Подключи репозиторий (замени URL на свой):
git remote add origin https://github.com/твой-username/твой-репозиторий.git
git branch -M main
git push -u origin main
```

### 2. Настрой Codemagic

1. **Зарегистрируйся:** [codemagic.io](https://codemagic.io)
2. **Подключи GitHub:**
   - Нажми "Sign up with GitHub"
   - Разреши доступ к репозиторию
   - Выбери "Только выбранные репозитории"
   - Выбери свой репозиторий `PectoranAPP`
3. **Добавь приложение:**
   - Нажми "Add application"
   - Выбери репозиторий `PectoranAPP`
   - Выбери "React Native" как тип проекта
   - Codemagic автоматически найдет `codemagic.yaml`
4. **Запусти сборку:**
   - Нажми "Start new build"
   - Выбери workflow "iOS Workflow"
   - Нажми "Start build"
5. **Скачай IPA:**
   - Дождись завершения (10-15 минут)
   - Скачай IPA из артефактов

### 3. Установи на iPhone через 3uTools

1. **Скачай 3uTools:** [3u.com](https://www.3u.com)
2. **Установи на Windows**
3. **Подключи iPhone через USB**
4. **Установи IPA:**
   - Открой 3uTools
   - Перейди в "Apps" → "Install"
   - Выбери IPA файл
   - Нажми "Install"
5. **На iPhone:**
   - Настройки → Основные → VPN и управление устройством
   - Доверься разработчику

**Готово! Приложение на iPhone.**

---

## Важно

- ✅ Android сборка **НЕ затронута** - все работает как раньше
- ✅ Codemagic использует только iOS workflow
- ✅ Все настройки в `codemagic.yaml` (уже создан)

---

## Если что-то не работает

**Ошибка при сборке:**
- Проверь логи в Codemagic
- Убедись, что все зависимости установлены
- Проверь, что `codemagic.yaml` правильный

**Не могу скачать IPA:**
- Проверь артефакты в Codemagic
- Может быть нужна подпись (добавь Apple Developer credentials)

**Android сломался:**
- Android **НЕ должен сломаться** - Codemagic только для iOS
- Если сломался - откати изменения в Git

